# Railway supplies PORT automatically; do not hardcode it.
PORT=8000
# Use a Railway persistent volume mount for production SQLite.
SICL_DB_PATH=/data/sicl/sicl.sqlite
# Comma-separated allowed browser origins.
SICL_CORS_ORIGINS=https://<frontend-host>
